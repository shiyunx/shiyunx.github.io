title:
author: GitHub User

copyright: "Unpublished Work (cleft) 2017-{currentYear} {author}"

yat:
  date_format: "%b %d, %Y"

markdown: kramdown
kramdown:
  input: GFM

plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap
  - jekyll-paginate
  - jekyll-spaceship
